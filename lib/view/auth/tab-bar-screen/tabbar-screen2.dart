

import '../../../controller/linker/linker.dart';

class TabBarscreen2 extends StatelessWidget {
  const TabBarscreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: MyText(txt: 'Screen2',))
    );
  }
}