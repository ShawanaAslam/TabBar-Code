

import '../../../controller/linker/linker.dart';

class TabBarscreen1 extends StatelessWidget {
  const TabBarscreen1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: MyText(txt: 'Screen1',)),
    );
  }
}
